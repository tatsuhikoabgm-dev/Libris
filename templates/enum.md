##  概要
Enum `{EnumName}` を作成する。

##  方針
- DB のコード値と 1:1 で対応付ける
- 表示名（label）が必要なら enum コンストラクタで注入

##  要件
- Enum `{EnumName}` を作成
- `code` / `label` を持たせる場合はコンストラクタを利用
- 想定される全値を網羅する

##  完了条件
- Enum クラス作成済み
- すべての値が揃っている
